package com.nusg.app;

import java.util.ArrayList;
import java.util.List;

import android.app.Activity;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.CheckBox;

public class PreAcceptance_InventoryList extends Activity{
/*
	private List <CheckBox> preAccCheckList = new ArrayList<CheckBox>();
	
	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.preacceptance);
		
		
		
		Button addNew = (Button) findViewById(com.nusg.app.R.id.addNewChecklistItem);
		addNew.setOnClickListener(new View.OnClickListener() {
			
			
			populateCheckList();
			
			@Override
			public void onClick(View arg) {
			
				CheckBox newCheckBox = new CheckBox(this);
				
			}
		});
	}
	
	private void populateCheckList(){
		preAccCheckList.add(new CheckBox("sxxfs"));
		preAccCheckList.add();
		preAccCheckList.add();
		preAccCheckList.add();
		preAccCheckList.add();
		preAccCheckList.add();
	}
	*/
}
